zuul:
  sensitive-headers:
  host:
    max-total-connections: 10000
    max-per-route-connections: 10000
  semaphore:
    max-semaphores: 200
  routes:
    config-server: /config/xxx
    service-api: /service/api
